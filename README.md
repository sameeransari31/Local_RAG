# Local_RAG
fully local Retrieval-Augmented Generation (RAG) system built from scratch using Python. Implements document ingestion, embedding, vector storage, and query-time retrieval with LLM-based response generation no external APIs, fully offline and customizable.
